---
phase: 02-multi-build-tracking-fan-out
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - index.js
  - __tests__/sound-monitor.spec.js
autonomous: true
requirements:
  - MULTI-02
  - MULTI-03
  - MULTI-05
  - MULTI-06
must_haves:
  truths:
    - "A tracked suite is deleted only after an observed success, failure, cancelled, or skipped transition has produced its title-identified announcement per D-05"
    - "A tracked id missing from a successful scrape remains in the Map without fabricated output, and no miss-count or timeout pruning exists per D-01 and D-03"
    - "A terminal-only historical row is silent, while the same id can be admitted and announced again after it later appears queued/running per D-04 and D-08"
    - "Tracked action_required changes announce and remain tracked; first-seen action_required is ignored per D-06"
    - "Tracked unknown changes remain stored and silent; first-seen unknown is ignored per D-07"
    - "Stored titles refresh on every observed tracked row so later output uses the newest title per D-10"
  artifacts:
    - path: "index.js"
      provides: "Complete InFlightBuildStore lifecycle matrix for admission, retention, transition, retirement, and re-admission"
      contains: "isTerminal"
    - path: "__tests__/sound-monitor.spec.js"
      provides: "Complete lifecycle matrix covering D-01 through D-10 and MULTI-05/MULTI-06"
      contains: "action_required"
  key_links:
    - from: "InFlightBuildStore.apply(states)"
      to: "isTerminal(status)"
      via: "derive descriptor before Map.delete(id)"
      pattern: "isTerminal"
    - from: "successful snapshot omission"
      to: "statesById"
      via: "only incoming rows are processed; no sweep deletes absent keys"
      pattern: "for .*states"
    - from: "tracked incoming BuildState"
      to: "later announcement statement"
      via: "store incoming snapshot even when status is unchanged or unknown"
      pattern: "statesById\\.set"
---

## Phase Goal

**As a** developer with several GitHub Actions workflows in flight, **I want to** hear every meaningful per-build status change identified by its run title, **so that** a non-top-row failure or completion is never silently missed.

<objective>
Complete the in-flight lifecycle matrix after the tracer: observed terminal retirement, safe absence retention, re-admission, attention/unknown semantics, and latest-title announcements.

Purpose: Finish MULTI-05/MULTI-06 and every remaining locked decision D-01 through D-10 while preserving the all-suite fan-out proven in plan 02-01.
Output: A fully specified and tested Map lifecycle in the existing `index.js` monolith.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-multi-build-tracking-fan-out/02-CONTEXT.md
@.planning/phases/02-multi-build-tracking-fan-out/02-RESEARCH.md
@.planning/phases/02-multi-build-tracking-fan-out/02-PATTERNS.md
@.planning/phases/02-multi-build-tracking-fan-out/02-VALIDATION.md
@.planning/phases/02-multi-build-tracking-fan-out/02-01-SUMMARY.md
@index.js
@__tests__/sound-monitor.spec.js

<interfaces>
Plan 02-01 contracts consumed here:
- `InFlightBuildStore() -> { apply(states), has(id), get(id), size }` with a private `Map<checkSuiteId, BuildState>`.
- `apply(BuildState[]) -> Array<{ statement, colorCode }>` in input/scrape order.
- `BuildState.buildName` is lifecycle identity; `BuildState.gitLog` is untrusted display identity.
- `isInFlight(status)` accepts queued/running; `isTerminal(status)` classifies success/failure/cancelled/skipped.
- `actionSoundJob` guards null before calling `apply` and fans descriptors to the existing announce callback.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Lock terminal retirement, absence retention, and re-admission</name>
  <files>index.js, __tests__/sound-monitor.spec.js</files>
  <read_first>
    - .planning/phases/02-multi-build-tracking-fan-out/02-01-SUMMARY.md: actual tracer exports and any execution deviations
    - index.js: InFlightBuildStore.apply, isInFlight, isTerminal, BuildState title-aware sentence behavior
    - __tests__/sound-monitor.spec.js: fresh-store setup, suite fixture helpers, ordered descriptor assertions
    - .planning/phases/02-multi-build-tracking-fan-out/02-CONTEXT.md: D-01, D-03, D-04, D-05, D-08
  </read_first>
  <behavior>
    - For each of success, failure, cancelled, and skipped: tracked running → terminal emits exactly one title-bearing descriptor and deletes the id
    - Reapplying the same terminal snapshot after deletion is silent and does not recreate the id
    - A later queued/running snapshot for that deleted id is admitted and announced as a new build
    - A tracked id omitted from a successful snapshot containing another valid suite remains present and emits nothing
    - First-seen completed historical rows remain silent and absent from the Map
  </behavior>
  <action>
    Write the complete retirement tests first, then make the store satisfy them:
    1. Add a table-driven lifecycle matrix for all four D-05 terminal statuses. For each case, admit a running suite, apply the terminal state, assert one descriptor includes the latest title and expected phrase/color, then assert `has(id)` is false.
    2. Reapply terminal-only snapshots after deletion and assert no descriptor/no admission per D-04. Then apply queued and running reappearances of the same id and assert fresh admission plus a new-build announcement per D-08 and MULTI-06. Keep no completed-id history outside the in-flight Map.
    3. Seed two ids, apply a successful snapshot containing only one, and assert the omitted id remains unchanged with no invented announcement per D-01. Implement this by processing incoming rows only; do not sweep, age, count misses, or prune omitted keys per D-03.
    4. In `InFlightBuildStore.apply`, derive a tracked terminal descriptor before deleting the id. Ensure terminal retirement happens even if the current title differs from the prior title, and that emitted text uses the incoming title.
    5. Keep admission restricted to queued/running and lifecycle comparisons enum-only. Do not add timers, timestamps, persistence, terminal blacklists, or absence-as-completion logic.
    <reversibility rating="costly">D-01 absence retention plus D-04/D-05 admission and terminal sets define when user-visible lifecycle state is kept or destroyed.</reversibility>
  </action>
  <verify>
    <automated>nix shell nixpkgs#nodejs_24 nixpkgs#pnpm --command pnpm exec jest --runInBand --testNamePattern="terminal|re-admit|absence|historical"</automated>
  </verify>
  <acceptance_criteria>
    - Every D-05 terminal status announces once for a tracked id and then removes it.
    - Historical terminal rows never enter the Map.
    - A deleted id is admitted and announced when later seen queued or running.
    - Successful-snapshot omission leaves tracked state untouched and silent.
    - No auxiliary blacklist, miss counter, timeout, or absent-id deletion path exists.
  </acceptance_criteria>
  <done>Terminal states retire only after observed output, absence never fabricates completion, and completed ids can re-enter the in-flight lifecycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Complete attention, unknown, title-refresh, and order semantics</name>
  <files>index.js, __tests__/sound-monitor.spec.js</files>
  <read_first>
    - index.js: InFlightBuildStore.apply, BuildState.diffToSentence, englishDictionary, ordered actionSoundJob loop
    - __tests__/sound-monitor.spec.js: lifecycle matrix and two-suite fan-out tests
    - .planning/phases/02-multi-build-tracking-fan-out/02-CONTEXT.md: D-06, D-07, D-09, D-10
    - .planning/phases/01-crash-guard-status-normalization/01-02-SUMMARY.md: unknown empty-statement invariant
  </read_first>
  <behavior>
    - First-seen action_required and unknown rows are silent and absent
    - Tracked running → action_required emits one title-bearing descriptor and retains the incoming snapshot
    - Tracked running → unknown emits no descriptor and retains the incoming snapshot
    - running(title A) → running(title B) emits nothing but stores title B; later terminal output names title B
    - Reversing two changed rows reverses descriptor and announce order without changing per-id results
    - Same-status tracked rows emit nothing while still refreshing their stored titles
  </behavior>
  <action>
    Complete the non-terminal tracked-state matrix with tests first:
    1. Add first-seen and tracked-transition pairs for `Status.ACTION_REQUIRED` and `Status.UNKNOWN`. D-06 requires tracked action_required changes to announce and remain tracked; D-07 requires tracked unknown to remain stored but produce no descriptor. Neither status can first-admit.
    2. Add the D-10 sequence running(title A) → running(title B) → terminal(title B). The same-status middle update must remain silent while replacing the stored snapshot, and the terminal descriptor must identify title B.
    3. Add a two-id transition test that runs the same states in A/B and B/A input order. Assert descriptor order and injected announce call order exactly follow the input each time per D-09; never sort by id/status or coalesce.
    4. Refine `apply` so every observed tracked row refreshes the stored snapshot before/while applying its status rule, except terminal rows which are announced then deleted. Preserve unknown's established empty-statement invariant and do not call `say` for empty descriptors.
    5. Run the entire suite and retain Phase 1 normalization/null behavior, Plan 02-01 all-row fan-out, and the import-time timer cleanup. Keep `say` shell execution and module layout unchanged for Phase 3 SAFE-01/SAFE-02/STRUCT-01.
  </action>
  <verify>
    <automated>nix shell nixpkgs#nodejs_24 nixpkgs#pnpm --command pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - action_required is tracked-only, announces changes, and remains in the Map.
    - unknown is tracked-only, remains in the Map, and never produces speech.
    - Same-status observations refresh titles without announcements.
    - Later transitions use the latest observed title.
    - Multi-change descriptor/callback order equals scrape order in both tested permutations.
    - Full Jest suite passes without invoking real macOS speech.
  </acceptance_criteria>
  <done>The Map state machine implements all D-06/D-07/D-09/D-10 edge semantics and all Phase 2 automated tests pass.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- `index.js`: complete in-flight-only lifecycle engine inside the existing monolith, including terminal retirement, absence retention, re-admission, attention/unknown handling, title refresh, and deterministic fan-out.
- `__tests__/sound-monitor.spec.js`: full lifecycle matrix for MULTI-01 through MULTI-06 and D-01 through D-10.
- `.planning/phases/02-multi-build-tracking-fan-out/02-02-SUMMARY.md`: executor record of completed Phase 2 behavior and verification.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Successful scrape snapshot → retained Map | Partial visibility must not erase still-running state |
| Incoming status → lifecycle classifier | Only the closed Status enum may control admission/retirement |
| Stored title → announcement | Latest untrusted display text is output, but never used as identity |
| Announcement descriptor → say | Inherited shell-backed speech remains an open Phase 3 boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | absence handling | high | mitigate | Keep absent ids until an observed D-05 terminal state; tests prove omission is silent and non-destructive |
| T-02-06 | Tampering | terminal/admission classification | high | mitigate | Enum-only predicates; full table tests; descriptor derived before deletion |
| T-02-07 | Denial of Service | retained vanished ids | low | accept | D-03 explicitly selects keep-until-terminal with no pruning; process-local Map remains bounded by observed admitted ids during a run |
| T-02-08 | Repudiation | stale title or reordered output | medium | mitigate | Refresh every observed tracked snapshot and assert exact two-order permutations |
| T-02-03 | Elevation of Privilege | inherited `say` via shell string | high | transfer | SAFE-01 remains Phase 3; this plan preserves the existing announce boundary and introduces no additional shell expansion |
| T-02-SC | Tampering | package supply chain | low | accept | No package install or dependency change occurs |
</threat_model>

<verification>
Automated: `nix shell nixpkgs#nodejs_24 nixpkgs#pnpm --command pnpm test`.
Human check (end of phase, optional): from the macOS host shell, watch a busy public Actions URL with at least two overlapping runs and confirm each title-identified change is heard. The automated fixture suite is the required closure gate because `say` is unavailable inside the isolated Nix shell.
</verification>

<success_criteria>
- MULTI-05: tracked terminal transitions announce once, then remove the id.
- MULTI-06: a removed id can be admitted and announced again as queued/running.
- D-01/D-03: omission never acts as terminal and no miss-based pruning exists.
- D-06/D-07: action_required and unknown follow their exact tracked-only retention/speech rules.
- D-10: every observed tracked row refreshes title state used by later output.
- Full Jest suite is green with all MULTI requirements covered.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Hear every meaningful change for several in-flight workflows, title-identified, then drop after terminal announce | 02-01, 02-02 | COVERED | Tracer proves fan-out; lifecycle expansion proves retirement |
| REQ | MULTI-01 | Scrape all check-suite rows | 02-01 | COVERED | Ordered querySelectorAll plus malformed-row isolation |
| REQ | MULTI-02 | Track queued/running suites by check-suite id | 02-01, 02-02 | COVERED | Private Map and admission matrix |
| REQ | MULTI-03 | Announce tracked changes with commit/run title | 02-01, 02-02 | COVERED | Title-bearing descriptors and refresh tests |
| REQ | MULTI-04 | Fan out every change in one poll | 02-01 | COVERED | Two-suite E2E and DOM-order assertions |
| REQ | MULTI-05 | Announce terminal then drop | 02-02 | COVERED | Four-status table |
| REQ | MULTI-06 | Re-admit a completed id | 02-02 | COVERED | Terminal → queued/running matrix |
| RESEARCH | — | Scrape-all/track-few, Map-owned state machine, compute-then-fan-out | 02-01 | COVERED | Recommended architecture used in monolith |
| RESEARCH | — | Null/all-malformed no-op and malformed sibling isolation | 02-01 | COVERED | Task 2 |
| RESEARCH | — | Terminal, absence, attention, unknown, title-refresh lifecycle matrix | 02-02 | COVERED | Both tasks |
| RESEARCH | — | No new dependency; preserve CommonJS/Jest/timer/say seams | 02-01, 02-02 | COVERED | Explicit task scope fences |
| CONTEXT | D-01 | Keep successfully omitted tracked id | 02-02 | COVERED | Task 1 |
| CONTEXT | D-02 | Null scrape skips store update | 02-01 | COVERED | Task 2 |
| CONTEXT | D-03 | No N-miss/timeout prune | 02-02 | COVERED | Task 1 |
| CONTEXT | D-04 | Admit queued/running only | 02-01, 02-02 | COVERED | Tracer plus historical-terminal tests |
| CONTEXT | D-05 | Four terminal statuses announce then delete | 02-02 | COVERED | Task 1 |
| CONTEXT | D-06 | Tracked-only action_required announces and stays | 02-02 | COVERED | Task 2 |
| CONTEXT | D-07 | Tracked unknown stays silent; never first-admit | 02-02 | COVERED | Task 2 |
| CONTEXT | D-08 | Announce every first admission | 02-01, 02-02 | COVERED | Tracer and re-admission |
| CONTEXT | D-09 | One announcement per change in DOM order | 02-01, 02-02 | COVERED | Tracer and permutation test |
| CONTEXT | D-10 | Refresh title on every successful per-id scrape | 02-02 | COVERED | Task 2 |

Deferred CONTEXT items (`execFile`, `require.main`, module split, N-miss pruning, speech queue, API/filter/TUI work) are intentionally excluded from Phase 2 coverage.

<output>
Create `.planning/phases/02-multi-build-tracking-fan-out/02-02-SUMMARY.md` when done.
</output>
