---
phase: 01-crash-guard-status-normalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.js
  - __tests__/sound-monitor.spec.js
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
  - REL-04
must_haves:
  truths:
    - "When got rejects or check_suite DOM is missing, buildState resolves to null and the poll handler does not call MostRecentUpdate"
    - "A successful scrape stores BuildState.status as a Status enum member (e.g. success), never the raw aria-label string"
    - "For status success, colorCode() returns a defined ANSI string and englishDictionary phrases use the enum key"
    - "Unit tests for this slice assert enum statuses and REL-01 null returns; afterAll still clearInterval(timer) per D-08"
  artifacts:
    - path: "index.js"
      provides: "Status, normalizeStatus, null-safe buildState, actionSoundJob guard, enum-keyed colorCode/englishDictionary"
      contains: "normalizeStatus"
    - path: "__tests__/sound-monitor.spec.js"
      provides: "REL-01 null tests + scrape→enum success assertion + enum-based announce tests for the tracer path"
      contains: "toBeNull"
  key_links:
    - from: "buildState"
      to: "normalizeStatus(ariaLabel)"
      via: "scrape boundary before BuildState construction"
      pattern: "normalizeStatus"
    - from: "actionSoundJob .then"
      to: "mostRecentUpdate"
      via: "early return when newState == null"
      pattern: "newState == null"
---

## Phase Goal

**As a** developer watching GitHub Actions, **I want to** keep the poller running through bad scrapes and live status labels, **so that** colors and speech stay defined without wiping prior state.

<objective>
Prove the walking-skeleton reliability path: scrape failure is a no-op update, and one live-shaped success aria-label normalizes to Status.SUCCESS through color and English speech.

Purpose: Lock D-01–D-03 and the costly enum shape early so Phase 2 lifecycle predicates share one vocabulary.
Output: Null-safe poll tick + Status/normalizeStatus wired into buildState; tracer tests green under pnpm test.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-crash-guard-status-normalization/01-CONTEXT.md
@.planning/phases/01-crash-guard-status-normalization/01-RESEARCH.md
@.planning/phases/01-crash-guard-status-normalization/SKELETON.md
@index.js
@__tests__/sound-monitor.spec.js
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end crash-safe scrape → enum success announce</name>
  <files>index.js</files>
  <behavior>
    - buildState resolves to null when got rejects (network failure)
    - buildState resolves to null when HTML has no check_suite row (or missing svg/title/aria)
    - buildState on the existing fixture (bare completed successfully) returns status equal to Status.SUCCESS (string value success)
    - BuildState with status success has defined colorCode() and englishDictionary.translate(success) yields a real phrase (not raw interpolation of the aria-label)
    - actionSoundJob .then returns before mostRecentUpdate when newState is null (per D-01)
  </behavior>
  <action>
    Implement the Phase 1 tracer slice in index.js only (per D-01, D-02, D-03, D-06; enum shape per RESEARCH Pattern 2 / Claude discretion A1+A4):
    1. Add a frozen Status map with string values: queued, running, success, failure, cancelled, skipped, action_required, unknown.
    2. Add normalizeStatus(ariaLabel): trim; take status head as text before the first colon when present, else the whole string; lower-case; map heads: queued → queued; currently running → running; completed successfully → success; failed → failure; cancelled/canceled substring → cancelled; skipped → skipped; requires action substring → action_required; else console.error unrecognized label and return unknown (per D-04, D-05).
    3. Rewrite buildState: try/got/JSDOM; query first [id^='check_suite_']; prefer svg[aria-label]; null-check row, svg, aria-label, and Link--primary title — any miss returns null; on success return new BuildState(row.id, normalizeStatus(aria), title text); catch logs err and returns null (explicit null, not bare undefined).
    4. Rewrite colorCode() and englishDictionary.translate to key only on Status string values for at least success (and preferably the other known members from RESEARCH recommended phrases/colors so the map is complete). Retire colon-suffixed colorCode keys. For unknown, colorCode returns undefined; omit unknown from speech phrase map so translate does not invent a spoken word for unknown (per D-05).
    5. Update diffToSentence to compare against Status.QUEUED (value queued) for the queued-noise skip, and to pass enum status into dictionary.translate.
    6. In actionSoundJob .then: if newState == null return immediately — never call mostRecentUpdate/say on failure (per D-01, D-02).
    7. Export Status and normalizeStatus from module.exports alongside existing exports. Keep setInterval import side-effect and timer export (D-08 / Phase 3 defer). Do not add dependencies; do not querySelectorAll / Map / execFile / require.main (deferred).
    <reversibility rating="costly">Status enum + normalize-at-boundary shape (D-03/D-04/D-06) — color, speech, and later lifecycle all depend on these string values.</reversibility>
  </action>
  <verify>
    <automated>nix-shell -p nodejs --run 'pnpm test' && grep -n "normalizeStatus\|return null\|newState == null" index.js | head -40</automated>
  </verify>
  <done>buildState returns null on scrape failure; success scrape path stores enum status; colorCode/englishDictionary consume enum; actionSoundJob skips update on null; Status and normalizeStatus exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Tracer tests: REL-01 nulls + scrape→enum + enum announce</name>
  <files>__tests__/sound-monitor.spec.js</files>
  <behavior>
    - got.mockRejectedValue → buildState resolves null
    - empty HTML without check_suite → buildState resolves null
    - existing fixture scrape expects status success (enum), not raw aria-label text
    - MostRecentUpdate / diffToSentence constructions use enum values (success, running) and expect defined colorCode for known statuses
    - afterAll still clearInterval(timer)
  </behavior>
  <action>
    Rewrite __tests__/sound-monitor.spec.js for the tracer path (per D-07, D-08, REL-01, REL-04):
    1. Import Status (and optionally normalizeStatus) from index.js; keep jest.mock(got) and afterAll clearInterval(timer).
    2. Add tests: got reject → null; HTML body with no check_suite → null.
    3. Change scrape happy-path expectation from raw completed successfully string to Status.SUCCESS / success; keep gitLog and buildName assertions.
    4. Replace dotted synthetic BuildState status literals in existing announce tests with Status enum values; expect colorCode to be defined (not undefined) for success/running transitions that announce.
    5. Do not add live GitHub network fetches. Leave additional live-shaped fixture variants for plan 01-02 if not already needed for the tracer green suite.
    Avoid inventing speech for unknown in these tests.
  </action>
  <verify>
    <automated>nix-shell -p nodejs --run 'pnpm test'</automated>
  </verify>
  <done>pnpm test green; REL-01 null cases present; scrape asserts enum success; announce tests use enum statuses with defined colors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions HTML → buildState | Untrusted scraped DOM/aria-label/title enter the process |
| BuildState.status → color/speech | Normalized enum must be the only control vocabulary |
| say(sentence) → macOS shell | Existing exec string spawn; not expanded this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | normalizeStatus / aria-label | medium | mitigate | Closed enum + unknown fallback; skip meaningless announce (D-04/D-05) |
| T-01-02 | Elevation of Privilege | say via exec | high | accept | Deferred to Phase 3 SAFE-01 (execFile); do not widen say surface in Phase 1 |
| T-01-03 | Information Disclosure | console.error on scrape failure | low | accept | Log Error objects as today; do not speak raw HTML |
| T-01-04 | Denial of Service | poll tick on scrape failure | medium | mitigate | Return null + call-site guard so interval does not throw (REL-01) |
| T-01-SC | Tampering | npm/pnpm installs | high | accept | No new packages this phase; legitimacy audit documents retain-only |
</threat_model>

<verification>
Automated: nix-shell -p nodejs --run 'pnpm test' passes after both tasks.
human-check: Optional end-of-phase smoke later — run monitor against a public Actions URL and confirm a failed network (airplane mode briefly) does not throw; not required to close this plan.
</verification>

<success_criteria>
- Scrape/network/DOM failure → null + no MostRecentUpdate mutation path
- Success fixture → status success with defined colorCode and English phrase
- Exports include Status and normalizeStatus
- Full Jest suite green
</success_criteria>

<output>
Create `.planning/phases/01-crash-guard-status-normalization/01-01-SUMMARY.md` when done
</output>
