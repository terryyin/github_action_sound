---
phase: 03-modular-cleanup-tech-debt
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - status.js
  - scrape.js
  - store.js
  - index.js
  - __tests__/sound-monitor.spec.js
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - STRUCT-01

must_haves:
  truths:
    - "D-01/D-02: Scrape, status, in-flight store, and announce responsibilities live in four root-level CommonJS modules behind a thin CLI."
    - "D-03: All Phase 1 and Phase 2 scrape, normalization, lifecycle, title-refresh, retirement, re-admission, and ordered fan-out behavior remains unchanged."
    - "D-05: The index.js barrel re-exports the established library surface without starting process lifecycle work."
    - "D-10: Only the wired English dictionary remains; the unused Japanese dictionary is removed."
  artifacts:
    - path: "status.js"
      provides: "Status vocabulary, normalization, BuildState descriptors, English dictionary, and color helpers"
      exports: ["Status", "normalizeStatus", "BuildState", "englishDictionary"]
    - path: "scrape.js"
      provides: "Public Actions HTML fetch and ordered normalized BuildState snapshots"
      exports: ["buildStates"]
    - path: "store.js"
      provides: "Map-backed in-flight lifecycle and classification predicates"
      exports: ["InFlightBuildStore", "isInFlight", "isTerminal"]
    - path: "index.js"
      provides: "Side-effect-free barrel and actionSoundJob composition"
      exports: ["buildStates", "BuildState", "say", "englishDictionary", "InFlightBuildStore", "actionSoundJob", "Status", "normalizeStatus"]
    - path: "__tests__/sound-monitor.spec.js"
      provides: "Full extracted-module and public-barrel regression coverage"
  key_links:
    - from: "scrape.js"
      to: "status.js"
      via: "BuildState construction after normalizeStatus at the DOM boundary"
      pattern: "new BuildState.*normalizeStatus"
    - from: "store.js"
      to: "status.js"
      via: "Status predicates and English BuildState descriptors"
      pattern: "require\\(['\"]\\./status['\"]\\)"
    - from: "index.js"
      to: "scrape.js"
      via: "actionSoundJob obtains ordered snapshots through buildStates"
      pattern: "buildStates\\(url\\)"
    - from: "index.js"
      to: "store.js"
      via: "Injected store applies each successful snapshot once"
      pattern: "store\\.apply\\(states\\)"
---

<objective>
**As a** developer watching GitHub Actions, **I want to** split scrape, status, store, and announce into clear modules, **so that** the proven multi-build behavior stays maintainable without changing announcements.

Expand the safe tracer into the locked four-module architecture. Move existing status, scrape, and lifecycle blocks into root siblings with a one-way dependency graph, remove dead locale code, and prove the complete Phase 2 matrix through the public import-safe barrel.

Purpose: Complete STRUCT-01 after the safety boundaries are proven, preserving D-03 behavior bit-for-bit.
Output: `status.js`, `scrape.js`, `store.js`, a pure `index.js` composition barrel, and comprehensive regression evidence.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-modular-cleanup-tech-debt/03-CONTEXT.md
@.planning/phases/03-modular-cleanup-tech-debt/03-RESEARCH.md
@.planning/phases/03-modular-cleanup-tech-debt/03-PATTERNS.md
@.planning/phases/02-multi-build-tracking-fan-out/02-VERIFICATION.md
@.planning/phases/03-modular-cleanup-tech-debt/03-01-SUMMARY.md
@index.js
@__tests__/sound-monitor.spec.js

<interfaces>
From Plan 03-01, `index.js` is import-safe and exports the established public symbols plus `actionSoundJob(url, announce, store)`. `cli.js` imports `actionSoundJob`, `InFlightBuildStore`, and `say` only through that barrel.

Create this one-way CommonJS graph:
- `status.js`: no project-module imports; exports `Status`, `normalizeStatus`, `BuildState`, and `englishDictionary`.
- `scrape.js`: imports `BuildState` and `normalizeStatus` from `./status`; exports `buildStates`.
- `store.js`: imports `Status`, `BuildState`, and `englishDictionary` from `./status`; exports `InFlightBuildStore`, `isInFlight`, and `isTerminal`.
- `announce.js`: remains the Plan 03-01 process adapter and imports no domain module.
- `index.js`: imports/re-exports the four module APIs and owns only injected `actionSoundJob` composition.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-02-01: Complete the four-module extraction without behavior drift</name>
  <reversibility rating="costly">D-01/D-03 establish module and public export boundaries used by the CLI and all tests; undoing them requires coordinated call-site changes.</reversibility>
  <files>status.js, scrape.js, store.js, index.js, __tests__/sound-monitor.spec.js</files>
  <read_first>
    - `03-CONTEXT.md` D-01, D-02, D-03, D-05, D-10, and D-11
    - `03-RESEARCH.md` Architectural Responsibility Map, one-way dependency graph, and Pitfall 3
    - `03-PATTERNS.md` exact extraction assignments for status.js, scrape.js, store.js, and index.js
    - `02-VERIFICATION.md` twelve observable truths that must remain satisfied
    - `03-01-SUMMARY.md` final import-safe barrel and CLI interfaces
    - `index.js` post-tracer implementations to relocate
    - `__tests__/sound-monitor.spec.js` existing 60-test regression surface and Plan 03-01 safety tests
  </read_first>
  <behavior>
    - status.js preserves the frozen eight-value Status enum, prefix normalization, enum-keyed colors/phrases, and unknown empty-statement behavior.
    - scrape.js preserves selectors, malformed-row isolation, null-on-unusable-poll behavior, normalization at scrape boundary, and valid DOM order.
    - store.js preserves suite-id Map identity, queued/running admission, tracked-only attention/unknown handling, title refresh, terminal retirement, absence retention, re-admission, and descriptor order.
    - index.js preserves null-scrape no-op and one announce callback per descriptor in returned order while remaining import-safe.
    - Direct module imports and the public barrel resolve to the same constructors/functions used by the complete regression suite.
  </behavior>
  <action>
    Before moving code, extend module-boundary assertions so the existing behavior is exercised through both direct root-level module imports and the package barrel. Extract the current implementations rather than redesigning them: place Status, statusHead, normalizeStatus, BuildState, required ANSI color constants, and englishDictionary in `status.js`; place got/JSDOM `buildStates` in `scrape.js`; place lifecycle predicates and InFlightBuildStore in `store.js`, all as named CommonJS exports per D-01/D-02.

    Keep normalization at the scrape boundary and preserve every selector, return value, iteration order, mutation order, sentence, color, and lifecycle predicate per D-03. Remove `japaneseDictionary` entirely per D-10. Reduce `index.js` to imports, the injected `actionSoundJob` composition, and named re-exports; it must not regain argv, singleton runtime state, or timer ownership per D-05.

    Keep the Plan 03-01 announce and CLI boundaries intact. Do not add URL allowlisting, a poll mutex, queueing, locale product behavior, selector rewrites, dependency changes, or quality-tooling expansion per D-11.
  </action>
  <verify>
    <automated>nix shell nixpkgs#nodejs_24 nixpkgs#pnpm --command pnpm test --runInBand</automated>
    <human-check>After automated verification, run required end-of-phase smoke on macOS: `nix shell nixpkgs#nodejs_24 nixpkgs#pnpm --command pnpm sound <actions-url>` (same argv shape as `github_action_sound <actions-url>`). Confirm a title-bearing transition is printed and spoken. Packaging/bin shebang is already gated by 03-01 automated tests; this smoke confirms the live poller path.</human-check>
  </verify>
  <acceptance_criteria>
    - All four root-level modules exist with the locked responsibilities and no circular dependency.
    - The public barrel retains all non-timer symbols used by existing consumers and tests.
    - Every Phase 1/2 regression and every Plan 03-01 safety test passes on Node 24.
    - No test clears an import-created interval, and importing each library module performs no network, polling, or speech work.
    - The unused Japanese dictionary no longer exists in runtime source or exports.
  </acceptance_criteria>
  <done>STRUCT-01 is complete: the four modules and thin CLI are wired through an inert library barrel, with SAFE-01/SAFE-02 and the full Phase 2 behavior matrix still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public GitHub HTML → `scrape.js` | Untrusted, structurally brittle HTML becomes normalized domain snapshots. |
| Domain snapshots → `store.js` | Ordered status/title observations mutate long-lived in-memory lifecycle state. |
| Library barrel → CLI/process adapters | Importing reusable code must not activate network, timer, or speech side effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | `scrape.js` HTML adapter | medium | mitigate | Preserve row validation, normalize at the boundary, skip malformed siblings, and return null for unusable snapshots before store mutation. |
| T-03-06 | Denial of Service | `store.js` lifecycle | medium | mitigate | Preserve D-03 null no-op, observed-terminal retirement, unknown handling, and deterministic mutation ordering under the existing matrix. |
| T-03-07 | Elevation of Privilege | `index.js` → `announce.js` | high | mitigate | Retain Plan 03-01 SAFE-01 argv-form process adapter and run its adversarial regression after extraction. |
| T-03-08 | Denial of Service | module import graph | high | mitigate | Keep process lifecycle exclusively in `cli.js`; direct-import tests assert no timer/network/speech activation from status, scrape, store, announce, or index. |
| T-03-09 | Tampering | cross-module refactor | medium | mitigate | Existing 60-test Phase 2 matrix plus direct-module identity assertions detect selector, status, ordering, and lifecycle drift. |
</threat_model>

<source_audit>
| Source | ID | Feature / Constraint | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | — | Shell-safe speech, inert library require, four clear modules, unchanged announce behavior | 03-01, 03-02 | COVERED |
| REQ | SAFE-01 | Scraped text never enters a shell string | 03-01, 03-02 regression | COVERED |
| REQ | SAFE-02 | Library require starts no poller or network loop | 03-01, 03-02 regression | COVERED |
| REQ | STRUCT-01 | Scrape/status/store/announce split from monolith | 03-01, 03-02 | COVERED |
| RESEARCH | — | Four root-level CJS modules with one-way dependencies and pure barrel | 03-01, 03-02 | COVERED |
| RESEARCH | — | Exact execFile argv test, import-side-effect test, package-entry assertion | 03-01 | COVERED |
| RESEARCH | — | Preserve complete Phase 2 matrix under Node 24/Nix | 03-02 | COVERED |
| CONTEXT | D-01 | Four modules plus thin CLI | 03-01, 03-02 | COVERED |
| CONTEXT | D-02 | Root-level CommonJS siblings | 03-01, 03-02 | COVERED |
| CONTEXT | D-03 | Preserve Phase 2 behavior bit-for-bit | 03-01, 03-02 | COVERED |
| CONTEXT | D-04 | Separate cli.js owns polling | 03-01 | COVERED |
| CONTEXT | D-05 | Side-effect-free main/barrel; bin and sound script use CLI | 03-01, 03-02 | COVERED |
| CONTEXT | D-06 | Remove timer cleanup mitigation | 03-01 | COVERED |
| CONTEXT | D-07 | execFile say argv invocation | 03-01 | COVERED |
| CONTEXT | D-08 | Overlapping speech remains | 03-01 | COVERED |
| CONTEXT | D-09 | Speech errors log and continue | 03-01 | COVERED |
| CONTEXT | D-10 | Remove unused Japanese dictionary | 03-02 | COVERED |
| CONTEXT | D-11 | Exclude deferred debt and product changes | 03-01, 03-02 | COVERED |
</source_audit>

<verification>
- `nix shell nixpkgs#nodejs_24 nixpkgs#pnpm --command pnpm test --runInBand`
- Verify test output includes the complete inherited suite plus SAFE-01, SAFE-02, manifest, and direct-module boundary assertions.
- Required end-of-phase macOS smoke: `nix shell nixpkgs#nodejs_24 nixpkgs#pnpm --command pnpm sound <public-actions-url>` preserves the public command shape and audible title-bearing transition.
</verification>

<success_criteria>
- SAFE-01 and SAFE-02 regressions remain green after every responsibility leaves the monolith.
- STRUCT-01 has exactly four root-level library responsibility modules plus the pure barrel and thin CLI.
- The complete normalized-status and multi-build lifecycle suite remains unchanged and green.
- The unused locale code is removed, while every D-11 deferred item remains outside implementation.
</success_criteria>

<output>
Create `.planning/phases/03-modular-cleanup-tech-debt/03-02-SUMMARY.md` when done.
</output>
