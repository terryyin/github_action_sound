---
phase: 01-crash-guard-status-normalization
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - index.js
  - __tests__/sound-monitor.spec.js
autonomous: true
requirements:
  - REL-02
  - REL-03
  - REL-04
must_haves:
  truths:
    - "Live-shaped aria-label with prefix colon Run suffix normalizes to the same enum as the bare prefix (e.g. success)"
    - "Fixture or constructed labels for queued, running, failure, cancelled, skipped, and action_required map to Status members with defined colorCode where phrases exist"
    - "unknown status does not crash; meaningless unknown transitions produce empty statement / no invented speech phrase"
    - "Test file constructions and expectations use Status enum values; REL-04 covered without live GitHub fetches"
  artifacts:
    - path: "index.js"
      provides: "Complete normalizeStatus mapping + enum-only color/speech including unknown skip"
      contains: "action_required"
    - path: "__tests__/sound-monitor.spec.js"
      provides: "Live-shaped + multi-status fixtures and normalizeStatus/scrape assertions"
      contains: "Run 1 of"
  key_links:
    - from: "normalizeStatus"
      to: "Status.*"
      via: "prefix head before colon"
      pattern: "statusHead|indexOf(':')"
    - from: "diffToSentence / colorCode"
      to: "englishDictionary"
      via: "enum keys only"
      pattern: "Status\\."
---

## Phase Goal

**As a** developer watching GitHub Actions, **I want to** keep the poller running through bad scrapes and live status labels, **so that** colors and speech stay defined without wiping prior state.

<objective>
Expand the tracer into full status coverage: complete prefix mapping, enum-only colors/phrases for all known members, unknown no-announce policy, and live-shaped fixtures locking the production aria-label contract.

Purpose: Finish REL-02/REL-03/REL-04 so Phase 2 can rely on Status for lifecycle predicates.
Output: Full mapping + fixture matrix; pnpm test green with enum-only assertions.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-crash-guard-status-normalization/01-CONTEXT.md
@.planning/phases/01-crash-guard-status-normalization/01-RESEARCH.md
@.planning/phases/01-crash-guard-status-normalization/01-01-SUMMARY.md
@index.js
@__tests__/sound-monitor.spec.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Complete enum mapping, colors, phrases, unknown skip</name>
  <files>index.js</files>
  <precondition>Plan 01-01 complete: Status, normalizeStatus, null-safe buildState, and actionSoundJob null guard exist in index.js</precondition>
  <behavior>
    - normalizeStatus maps bare and colon-suffixed heads for queued, currently running, completed successfully, failed, skipped, cancelled/canceled, requires action → corresponding Status members
    - Unrecognized label → Status.UNKNOWN with console.error; colorCode undefined; diffToSentence returns empty string for unknown→unknown or when dictionary has no phrase
    - colorCode defined for queued, running, success, failure, cancelled, skipped, action_required
    - englishDictionary keys are enum values (plus new_build / the_build); no speech key for unknown
  </behavior>
  <action>
    Finish index.js announce vocabulary against the locked Status shape (per D-04, D-05, D-06 and RESEARCH recommended phrases/colors):
    1. Ensure normalizeStatus covers all RESEARCH mapping rows including fixture-locked queued head (no live sample required) and cancelled via cancelled/canceled includes; action_required for requires action (discretion A1 — include as first-class member).
    2. Ensure colorCode map includes queued, running, success, failure, cancelled, skipped, action_required with RESEARCH ANSI combos; unknown absent → undefined.
    3. Ensure englishDictionary phrase table includes queued, running, success, failure, cancelled, skipped, action_required with RESEARCH wording; intentionally omit unknown so translate does not speak the word unknown (prefer empty statement via diffToSentence when status is unknown or phrase missing — log already happened in normalizeStatus).
    4. Confirm diffToSentence skips queued noise via Status.QUEUED and does not announce when statement would be empty.
    5. Do not change say/exec, poller import side-effect, or add multi-row scrape. Leave japaneseDictionary alone unless a one-line key align is trivial; English path is the REL-03 contract.
    <reversibility rating="costly">Enum member set and phrase/color maps (D-04/D-06).</reversibility>
  </action>
  <verify>
    <automated>nix-shell -p nodejs --run 'pnpm test'</automated>
  </verify>
  <done>All known Status members have color and English phrases except unknown; normalizeStatus mapping complete; unknown does not invent speech.</done>
</task>

<task type="auto" tdd="true">
  <name>Live-shaped fixtures and full REL-04 enum assertions</name>
  <files>__tests__/sound-monitor.spec.js</files>
  <behavior>
    - Fixture with aria-label completed successfully:  Run 1 of CI. title scrapes to status success
    - Direct normalizeStatus (or scrape) cases for queued, currently running, failed, skipped, cancelled, requires action heads (bare and/or live-shaped)
    - unknown input maps to unknown without throwing
    - No BuildState test constructions use synthetic dotted status strings that diverge from the enum
    - afterAll clearInterval(timer) retained
  </behavior>
  <action>
    Expand __tests__/sound-monitor.spec.js fixture coverage (per D-07, REL-02, REL-04):
    1. Add at least one live-shaped success fixture by cloning/replacing the existing svg aria-label with a colon+Run suffix form from RESEARCH (e.g. completed successfully followed by colon and Run N of Workflow. title); assert buildState.status === Status.SUCCESS and colorCode defined.
    2. Add focused normalizeStatus (exported) or small HTML snippets covering: queued, currently running, failed, skipped, cancelled, requires action with the application — prefer live-shaped where RESEARCH shows colon suffixes; fixture-lock queued even without live sample.
    3. Assert unrecognized label → Status.UNKNOWN; assert MostRecentUpdate/diff path for unknown does not throw and yields empty statement when appropriate (D-05).
    4. Sweep remaining tests so BuildState statuses are only Status enum values. Keep jest.mock(got) and clearInterval(timer) (D-08). No live GitHub fetches in CI.
  </action>
  <verify>
    <automated>nix-shell -p nodejs --run 'pnpm test'</automated>
  </verify>
  <done>Live-shaped and multi-status fixture tests pass; suite asserts enum only; pnpm test fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scraped aria-label → normalizeStatus | Untrusted label strings; must not drive control flow until enum |
| Enum → speech phrase | Missing phrase must not interpolate raw untrusted labels into say |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | unknown / missing phrase path | medium | mitigate | Skip speech when unknown or phrase absent; log unrecognized labels |
| T-01-06 | Information Disclosure | normalizeStatus console.error | low | accept | Logs raw aria-label for operator debug; not spoken |
| T-01-02 | Elevation of Privilege | say via exec | high | accept | Still deferred Phase 3 SAFE-01 |
| T-01-SC | Tampering | package installs | high | accept | No new packages |
</threat_model>

<verification>
Automated: nix-shell -p nodejs --run 'pnpm test'
human-check (end-of-phase): Spot-check that spoken lines for success/failure match enum phrases when watching a busy public Actions URL; confirm no undefined color for known statuses.
</verification>

<success_criteria>
- Live-shaped aria-labels normalize identically to bare prefixes
- All D-04 required statuses (+ action_required, cancelled, skipped) mapped
- unknown safe; REL-04 tests enum/fixtures only
- Full suite green
</success_criteria>

<output>
Create `.planning/phases/01-crash-guard-status-normalization/01-02-SUMMARY.md` when done
</output>
