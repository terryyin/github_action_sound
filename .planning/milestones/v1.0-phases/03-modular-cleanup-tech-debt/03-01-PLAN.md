---
phase: 03-modular-cleanup-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - announce.js
  - index.js
  - cli.js
  - package.json
  - __tests__/sound-monitor.spec.js
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - STRUCT-01

must_haves:
  truths:
    - "D-07: Scraped announcement text reaches macOS say as one argv item without a shell boundary."
    - "D-04/D-05/D-06: Requiring the package library starts no interval, network request, or speech process and needs no timer teardown."
    - "D-08/D-09: Speech remains asynchronous and overlapping; child-process failures are logged without stopping polling."
    - "The installed command and scripts.sound still start the 5-second poller through cli.js with the same URL argument shape."
  artifacts:
    - path: "announce.js"
      provides: "Side-effect adapter for timestamped stderr and argv-form macOS speech"
      exports: ["say", "now"]
    - path: "cli.js"
      provides: "Executable-only argv, store, and interval ownership"
    - path: "index.js"
      provides: "Import-safe CommonJS library barrel and injected actionSoundJob orchestration"
      exports: ["actionSoundJob", "say"]
    - path: "package.json"
      provides: "Library main and cli.js bin/script routing"
    - path: "__tests__/sound-monitor.spec.js"
      provides: "SAFE-01 and SAFE-02 boundary regression coverage"
  key_links:
    - from: "cli.js"
      to: "index.js"
      via: "CommonJS import of actionSoundJob, InFlightBuildStore, and say"
      pattern: "require\\(['\"]\\./index['\"]\\)"
    - from: "index.js"
      to: "announce.js"
      via: "Side-effect-free re-export and injected announcement callback"
      pattern: "require\\(['\"]\\./announce['\"]\\)"
    - from: "package.json"
      to: "cli.js"
      via: "bin.github_action_sound and scripts.sound"
      pattern: "\\./cli\\.js"
---

<objective>
**As a** developer watching GitHub Actions, **I want to** harden speech against shell injection and require the library without starting the poller, **so that** multi-build monitoring stays safe and reusable without changing announce behavior.

Deliver the production tracer from package CLI entry through import-safe orchestration to the macOS speech process boundary. This is the first vertical slice of D-01 and implements D-04 through D-09 while preserving the existing Phase 2 fan-out contract per D-03.

Purpose: Eliminate the two safety-critical side effects before the remaining domain modules are extracted.
Output: `announce.js`, `cli.js`, an import-safe `index.js`, retargeted package entries, and focused boundary tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-modular-cleanup-tech-debt/03-CONTEXT.md
@.planning/phases/03-modular-cleanup-tech-debt/03-RESEARCH.md
@.planning/phases/03-modular-cleanup-tech-debt/03-PATTERNS.md
@.planning/phases/02-multi-build-tracking-fan-out/02-01-SUMMARY.md
@.planning/phases/02-multi-build-tracking-fan-out/02-02-SUMMARY.md
@index.js
@package.json
@__tests__/sound-monitor.spec.js

<interfaces>
Current public library contract: `buildStates`, `BuildState`, `say`, `englishDictionary`, `InFlightBuildStore`, `actionSoundJob`, `Status`, and `normalizeStatus`. Preserve these named CommonJS exports; remove only the import-created `timer` contract per D-06.

`actionSoundJob(url, announce, store)` must continue to await `buildStates(url)`, return `[]` for null snapshots, apply the store once, invoke `announce(statement, colorCode)` once per descriptor in array order, and return the descriptors.

New announce contract: `say(sentence, colorCode)` returns immediately for an empty sentence, writes the existing timestamped colored line to stderr, and starts `execFile('say', [sentence], callback)` without awaiting it.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 03-01-01: Prove the safe CLI-to-speech path end to end</name>
  <reversibility rating="costly">D-04 and D-07 change the published executable boundary and security contract; undoing them would coordinate package consumers, tests, and process invocation.</reversibility>
  <files>announce.js, index.js, cli.js, package.json, __tests__/sound-monitor.spec.js</files>
  <read_first>
    - `03-CONTEXT.md` D-01, D-03, D-04, D-05, D-06, D-07, D-08, D-09, and D-11
    - `03-PATTERNS.md` assignments for announce.js, index.js, cli.js, package.json, and the Jest mock timing warning
    - `index.js` current `say`, module-scope argv/store/timer, `actionSoundJob`, and exports
    - `__tests__/sound-monitor.spec.js` top-level imports, got mock, timer teardown, and two-suite fan-out test
  </read_first>
  <behavior>
    - SAFE-01: a sentence containing quotes, command substitutions, semicolons, and newlines is passed unchanged as the sole element of the argv array for executable `say`.
    - SAFE-01/D-08/D-09: empty input starts no process; callback errors are written to stderr and do not throw or serialize later speech calls.
    - SAFE-02: requiring `index.js` invokes neither `setInterval`, `got`, nor the speech executor and exposes no timer cleanup handle.
    - D-04/D-05: `cli.js` alone reads argv, creates one InFlightBuildStore, and schedules `actionSoundJob(url, say, store)` every 5000ms.
    - D-03: an injected two-suite poll still fans out two title-bearing announcements in DOM order.
  </behavior>
  <action>
    Start with focused failing Jest assertions, mocking `child_process` before loading the announce module so CommonJS binding order is deterministic. Extract `now`, reset styling, stderr output, and `say` into root-level CommonJS `announce.js`; invoke `execFile` with executable `say`, one sentence argument, no shell option, and the existing best-effort callback per D-07/D-08/D-09.

    Move argv reading, one `InFlightBuildStore()` construction, and the 5000ms interval into executable root-level `cli.js` with the existing Node shebang per D-02/D-04. Make `index.js` declaration-only at require time: import/re-export `say`, retain the injected `actionSoundJob(url, announce, store)` contract, remove module-scope argv/default-store/timer creation and the timer export per D-05/D-06. Retarget only `bin.github_action_sound` and `scripts.sound` to `./cli.js` while keeping `main` at `index.js`.

    Preserve the current interval cadence, null-scrape no-op, descriptor order, title text, and overlapping callbacks per D-03/D-08. Do not add URL validation, a poll mutex, a speech queue, dependency/tooling changes, or other D-11 scope.
  </action>
  <verify>
    <automated>nix shell nixpkgs#nodejs_24 nixpkgs#pnpm --command pnpm test --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - Jest proves hostile scraped text is data in one argv item, not executable syntax.
    - Requiring `index.js` leaves timer, network, and speech mocks untouched and no `afterAll(clearInterval(...))` workaround remains.
    - The two-suite Phase 2 tracer remains green through the import-safe barrel.
    - `package.json` keeps `main: index.js` and maps both executable routes to `cli.js`.
  </acceptance_criteria>
  <done>The public CLI still schedules the existing multi-build job, package imports are inert, and speech crosses a direct argv boundary with Phase 2 fan-out unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-01-02: Lock best-effort announcement and package-entry behavior</name>
  <files>announce.js, package.json, __tests__/sound-monitor.spec.js</files>
  <read_first>
    - `03-CONTEXT.md` D-05, D-08, D-09, and D-11
    - `announce.js` interface produced by Task 03-01-01
    - `package.json` main/bin/scripts fields produced by Task 03-01-01
    - `__tests__/sound-monitor.spec.js` SAFE boundary tests produced by Task 03-01-01
  </read_first>
  <behavior>
    - Two immediate non-empty `say` calls start two child processes without waiting for either callback.
    - A process callback error is logged once while later calls remain possible.
    - Empty input writes no normal announcement and starts no child process.
    - Manifest assertions fail if main, bin, or scripts.sound drift away from their D-05 targets.
    - Public bin target `cli.js` exists, begins with `#!/usr/bin/env node`, and is the file `bin.github_action_sound` resolves to (deterministic packaging smoke for Roadmap criterion 4 without a live network poll).
  </behavior>
  <action>
    Expand the focused tests around the production tracer to cover D-08 overlap, D-09 log-and-continue, the empty-sentence guard, and the exact D-05 manifest routing. Also assert that the bin path file exists on disk and its first line is the Node shebang so an installed `github_action_sound` entry cannot silently point at a non-executable library barrel. Keep the adapter asynchronous and callback-based; retain the current timestamp/color stderr output. Use JSON manifest assertions rather than changing release metadata. Do not add locale selection, queueing, CLI validation, new dependencies, version sources, or changelog/release work per D-10/D-11 and the release rule. README needs no edit (research Q2 resolved).
  </action>
  <verify>
    <automated>nix shell nixpkgs#nodejs_24 nixpkgs#pnpm --command pnpm test --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - Overlap, callback error, and empty-input cases are covered without invoking the host `say` binary.
    - Package-entry tests cover `main`, `bin.github_action_sound`, and `scripts.sound`.
    - Bin smoke: `cli.js` exists with Node shebang and matches `package.json` `bin.github_action_sound`.
    - The full pre-existing reliability and multi-build suite remains green.
  </acceptance_criteria>
  <done>SAFE-01 and SAFE-02 have direct automated regression evidence, including best-effort speech and published entry routing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub HTML → announcement sentence | Public scraped title text is untrusted and may contain shell metacharacters. |
| Library import → process lifecycle | A consumer import must not trigger persistent timers, network traffic, or child processes. |
| CLI → macOS process | The executable adapter starts the platform `say` command with untrusted text as data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | `announce.js` speech invocation | high | mitigate | D-07 direct `execFile` call with the sentence as one argv item and no shell option; exact Jest assertion blocks regressions. |
| T-03-02 | Denial of Service | `index.js` import boundary | high | mitigate | D-04/D-05 move argv, store creation, and interval scheduling exclusively to `cli.js`; import-side-effect test spies on timer/network/speech. |
| T-03-03 | Denial of Service | `announce.js` callback | medium | mitigate | D-08/D-09 keep fire-and-forget overlap and log child-process errors without throwing into the poll loop. |
| T-03-04 | Tampering | `package.json` entry routing | medium | mitigate | Static manifest assertions bind library main to `index.js` and executable routes to `cli.js`. |
</threat_model>

<verification>
- `nix shell nixpkgs#nodejs_24 nixpkgs#pnpm --command pnpm test --runInBand`
- Confirm Jest exits without open-handle warnings and without invoking a real network request or `say` process.
</verification>

<success_criteria>
- SAFE-01 is proven with exact executable and argv assertions against adversarial title text.
- SAFE-02 is proven by inert library import and removal of timer teardown/export.
- The npm command and local sound script both route to the executable-only CLI.
- Existing Phase 2 ordered multi-build behavior remains green.
</success_criteria>

<output>
Create `.planning/phases/03-modular-cleanup-tech-debt/03-01-SUMMARY.md` when done.
</output>
